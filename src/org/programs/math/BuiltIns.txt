fn root(num, index) native

fn sqrt(num) = root(num, 2)

fn cbrt(num) = root(num, 3)

fn log(index, base=10) native

fn ln(index) = log(index, e)

fn sin(deg) native

fn cos(deg) native

fn tan(deg) native

fn cot(deg) = 1 / tan(deg)

fn sec(deg) = 1 / cos(deg)

fn cosec(deg) = 1 / sin(deg)

fn rad(deg) = deg * pi / 180

fn deg(rad) = rad * 180 / pi

fn approx(num, roundDecimals=0) native

fn floor(x) native

fn ceil(x) native
